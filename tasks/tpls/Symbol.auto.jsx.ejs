var React = require('react');
var Symbol = require('../Symbol.jsx');

var instance = (
    <div className='icon-grid'>
    <% for (var idx = 0; idx < files.length; idx++) {
    %>  <Symbol type='<%= files[idx] %>' title='<%= files[idx] %>'/>
    <% }
    %></div>
);

React.render(instance, mountNode);
